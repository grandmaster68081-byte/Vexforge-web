import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
    import { useColors } from '@/hooks/useColors';
    import { useGame } from '@/context/GameContext';
    export default function BattleScreen() { const colors = useColors(); const { session, opponents, findOpponents, startBattle, battleLoading, battleResult, authError, clearBattleResult } = useGame(); if (!session) return <View style={[styles.center, { backgroundColor: colors.background }]}><Text style={[styles.title, { color: colors.foreground }]}>Batalla PvP</Text><Text style={[styles.copy, { color: colors.mutedForeground }]}>Inicia sesión en Perfil para usar el combate real de VEXFORGE.</Text></View>; return <ScrollView style={{ backgroundColor: colors.background }} contentContainerStyle={styles.screen}><Text style={[styles.eyebrow, { color: colors.primary }]}>VEXFORGE / BATALLA</Text><Text style={[styles.title, { color: colors.foreground }]}>Arena oficial</Text><Text style={[styles.copy, { color: colors.mutedForeground }]}>Los resultados se resuelven con el RPC autoritativo de Supabase. No hay victorias simuladas en el dispositivo.</Text><Pressable onPress={findOpponents} style={[styles.button, { backgroundColor: colors.primary }]}><Text style={[styles.buttonText, { color: colors.background }]}>BUSCAR OPONENTES</Text></Pressable>{opponents.map((opponent) => <View key={opponent.player_id} style={[styles.opponent, { backgroundColor: colors.panel, borderColor: colors.border }]}><View style={{ flex: 1 }}><Text style={[styles.name, { color: colors.foreground }]}>{opponent.display_name}</Text><Text style={[styles.meta, { color: colors.mutedForeground }]}>MMR {opponent.mmr} · {opponent.wins}W / {opponent.losses}L</Text></View><Pressable disabled={battleLoading} onPress={() => startBattle(opponent.player_id)} style={[styles.smallButton, { backgroundColor: colors.panelStrong, borderColor: colors.border }]}>{battleLoading ? <ActivityIndicator color={colors.primary} /> : <Text style={[styles.smallText, { color: colors.primary }]}>BATALLAR</Text>}</Pressable></View>)}{battleResult ? <View style={[styles.result, { backgroundColor: colors.panel, borderColor: colors.primary }]}><Text style={[styles.name, { color: colors.foreground }]}>{battleResult.you_won ? 'Victoria confirmada' : 'Combate resuelto'}</Text><Text style={[styles.meta, { color: colors.mutedForeground }]}>Cambio de ELO: {battleResult.elo_change ?? 0}</Text><Pressable onPress={clearBattleResult}><Text style={[styles.link, { color: colors.primary }]}>Cerrar resultado</Text></Pressable></View> : null}{authError ? <Text style={[styles.error, { color: colors.destructive }]}>{authError}</Text> : null}</ScrollView>; }
    const styles = StyleSheet.create({ screen: { padding: 22, paddingTop: 54 }, center: { flex: 1, justifyContent: 'center', padding: 24 }, eyebrow: { fontSize: 11, fontWeight: '800', letterSpacing: 2 }, title: { fontSize: 28, fontWeight: '800', marginTop: 14 }, copy: { fontSize: 13, lineHeight: 20, marginTop: 8, marginBottom: 22 }, button: { minHeight: 50, borderRadius: 12, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 18, marginBottom: 16 }, buttonText: { fontSize: 12, fontWeight: '800', letterSpacing: 1 }, opponent: { flexDirection: 'row', alignItems: 'center', borderWidth: 1, borderRadius: 16, padding: 15, marginBottom: 10 }, name: { fontSize: 15, fontWeight: '800' }, meta: { fontSize: 12, marginTop: 6 }, smallButton: { minWidth: 92, minHeight: 40, borderWidth: 1, borderRadius: 10, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 10 }, smallText: { fontSize: 10, fontWeight: '800' }, result: { borderWidth: 1, borderRadius: 16, padding: 16, marginTop: 12 }, link: { marginTop: 14, fontWeight: '800' }, error: { marginTop: 16, lineHeight: 20 } });
    