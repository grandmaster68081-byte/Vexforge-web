import { ActivityIndicator, RefreshControl, ScrollView, StyleSheet, Text, View } from 'react-native';
    import { useColors } from '@/hooks/useColors';
    import { useGame } from '@/context/GameContext';
    export default function CollectionScreen() { const colors = useColors(); const { featuredCards, cardsTotal, syncState, refresh } = useGame(); return <ScrollView style={{ backgroundColor: colors.background }} contentContainerStyle={styles.screen} refreshControl={<RefreshControl refreshing={syncState === 'loading'} onRefresh={refresh} tintColor={colors.primary} />}><Text style={[styles.eyebrow, { color: colors.primary }]}>VEXFORGE / CARTAS</Text><Text style={[styles.title, { color: colors.foreground }]}>Catálogo oficial</Text><Text style={[styles.copy, { color: colors.mutedForeground }]}>{cardsTotal} cartas activas leídas desde Supabase.</Text>{syncState === 'loading' && featuredCards.length === 0 ? <ActivityIndicator color={colors.primary} /> : featuredCards.map((card) => <View key={card.id} style={[styles.card, { backgroundColor: colors.panel, borderColor: colors.border }]}><View style={styles.row}><Text style={[styles.name, { color: colors.foreground }]}>{card.name}</Text><Text style={[styles.rarity, { color: colors.primary }]}>{card.rarity ?? '—'}</Text></View><Text style={[styles.meta, { color: colors.mutedForeground }]}>{card.code} · {card.faction ?? 'Sin facción'} · Poder {card.power ?? '—'}</Text></View>)}</ScrollView>; }
    const styles = StyleSheet.create({ screen: { padding: 22, paddingTop: 54 }, eyebrow: { fontSize: 11, fontWeight: '800', letterSpacing: 2 }, title: { fontSize: 28, fontWeight: '800', marginTop: 14 }, copy: { fontSize: 13, lineHeight: 20, marginTop: 8, marginBottom: 22 }, card: { borderWidth: 1, borderRadius: 16, padding: 16, marginBottom: 10 }, row: { flexDirection: 'row', justifyContent: 'space-between', gap: 12 }, name: { fontSize: 16, fontWeight: '800', flex: 1 }, rarity: { fontSize: 11, fontWeight: '800', textTransform: 'uppercase' }, meta: { fontSize: 12, marginTop: 8 } });
    