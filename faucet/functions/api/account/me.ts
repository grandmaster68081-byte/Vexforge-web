import {currentAccount} from '../_lib/auth';import type {Env} from '../_lib/env';import {db} from '../_lib/supabase';import {json} from '../_lib/response';
export const onRequestGet=async({request,env}:{request:Request;env:Env})=>{const current=await currentAccount(request,env);if(!current)return json({user:null,wallet:null,csrfToken:null});const [{data:wallet},{data:session}]=await Promise.all([db(env).from('wallets').select('available_points,pending_points,lifetime_earned_points,lifetime_withdrawn_points,debt_points').eq('account_id',current.account.id).single(),db(env).from('sessions').select('csrf_hash').eq('id',current.session.sessionId).single()]);return json({user:{id:current.account.id,publicId:current.account.public_id,username:current.account.username,email:current.account.email,role:current.account.role},wallet:wallet?mapWallet(wallet):null,csrfToken:null});};function mapWallet(x:any){return {availablePoints:Number(x.available_points),pendingPoints:Number(x.pending_points),lifetimeEarnedPoints:Number(x.lifetime_earned_points),lifetimeWithdrawnPoints:Number(x.lifetime_withdrawn_points),debtPoints:Number(x.debt_points??0)}}
