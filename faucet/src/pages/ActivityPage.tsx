import { ArrowDownLeft, History, MinusCircle, PlusCircle, RotateCcw } from 'lucide-react';
import type { LedgerEntry } from '../lib/types';
import { points, shortDate } from '../lib/format';
export function ActivityPage({ledger}:{ledger:LedgerEntry[]}){return <div className="page-stack"><section className="section-heading page-title"><div><span className="kicker"><History size={12}/> ACCOUNT LEDGER</span><h1>Activity</h1><p>A readable audit trail of rewards, reservations, reversals and adjustments.</p></div></section><div className="table-card"><div className="table-scroll"><table><thead><tr><th>Time</th><th>Event</th><th>Reference</th><th>Change</th><th>Status</th></tr></thead><tbody>{ledger.map(e=><tr key={e.id}><td>{shortDate(e.createdAt)}</td><td><span className="activity-type">{e.type==='reward'?<PlusCircle size={14}/>:e.type==='withdrawal'?<ArrowDownLeft size={14}/>:e.type==='withdrawal_reversal'?<RotateCcw size={14}/>:<MinusCircle size={14}/>} {e.source}</span></td><td><code>{e.referenceId??'—'}</code></td><td className={e.pointsDelta>=0?'positive':'negative'}>{e.pointsDelta>=0?'+':''}{points(e.pointsDelta)} pts</td><td><span className={`status ${e.status}`}>{e.status}</span></td></tr>)}{!ledger.length&&<tr><td colSpan={5} className="table-empty">Complete your first verified opportunity and this ledger will begin filling automatically.</td></tr>}</tbody></table></div></div></div>}
