import { ArrowRight, LockKeyhole, Mail, UserRound } from 'lucide-react';
import { FormEvent, useState } from 'react';
import { Logo } from '../components/Logo';
export function AuthPage({mode,onSubmit,onSwitch}:{mode:'login'|'signup';onSubmit:(p:any)=>Promise<void>;onSwitch:(m:'login'|'signup')=>void}){
 const [username,setUsername]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');
 const submit=async(e:FormEvent)=>{e.preventDefault();setError('');setBusy(true);try{await onSubmit(mode==='login'?{email,password}:{username,email,password})}catch(err){setError(err instanceof Error?err.message:'Could not complete request.')}finally{setBusy(false)}};
 return <div className="auth-shell"><div className="auth-backdrop"><div className="auth-brand"><Logo/><span>PRIVATE REWARDS LAYER</span></div><div className="auth-quote"><span className="kicker">KIVORA</span><h1>Earn with a clearer path from task to reward.</h1><div className="auth-badges"><span>Verified callbacks</span><span>Internal points</span><span>Reviewed payouts</span></div></div></div><main className="auth-card"><div className="auth-mobile-brand"><Logo/></div><span className="kicker">{mode==='login'?'WELCOME BACK':'START EARNING'}</span><h2>{mode==='login'?'Sign in to Kivora':'Create your account'}</h2><p className="auth-sub">{mode==='login'?'Your rewards, activity and payout history are waiting.':'Free to join. Complete verified opportunities and build your Kivora balance.'}</p><form onSubmit={submit}>{mode==='signup'&&<label>Username<div className="input-icon"><UserRound size={16}/><input value={username} onChange={e=>setUsername(e.target.value)} placeholder="your_username" autoComplete="username"/></div></label>}<label>Email<div className="input-icon"><Mail size={16}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email"/></div></label><label>Password<div className="input-icon"><LockKeyhole size={16}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="8–128 characters" autoComplete={mode==='login'?'current-password':'new-password'}/></div></label>{error&&<div className="notice error">{error}</div>}<button className="primary-button full" disabled={busy}>{busy?'Signing you in…':mode==='login'?'Sign in':'Create account'} <ArrowRight size={17}/></button></form><div className="auth-switch">{mode==='login'?<>New to Kivora? <button onClick={()=>onSwitch('signup')}>Create an account</button></>:<>Already a member? <button onClick={()=>onSwitch('login')}>Sign in</button></>}</div><small className="legal-note">By continuing you agree to the Kivora Terms and Privacy Notice. Rewards and campaign availability vary by market and provider inventory.</small></main></div>;
}
