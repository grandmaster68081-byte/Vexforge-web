export const points=(n:number)=>new Intl.NumberFormat(undefined,{maximumFractionDigits:2}).format(n);export const usdFromPoints=(n:number,perUsd:number)=>perUsd>0?n/perUsd:0;export const shortDate=(s:string)=>new Intl.DateTimeFormat(undefined,{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}).format(new Date(s));export const maskAddress=(x:string)=>x.length>16?`${x.slice(0,7)}…${x.slice(-7)}`:x;
