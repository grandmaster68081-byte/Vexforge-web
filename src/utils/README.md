# utils/

Reserved for pure, domain-agnostic helpers (formatting, date math, etc.) that
more than one domain needs. Currently empty — no such duplication exists yet
across `src/domains/*`. Add a function here only once it's actually
duplicated in two domains, not preemptively.
